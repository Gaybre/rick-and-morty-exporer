import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4abc21',
      dark: '#43ab1d',
      contrastText: '#fcfcfc',
    },
    secondary: {
      main: '#00afd6',
    },
  },
})
