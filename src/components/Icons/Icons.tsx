import {
  DeleteForeverOutlined as Delete,
  Search,
  FilterAltOutlined as Filter,
} from '@mui/icons-material'

export const icons = {
  delete: <Delete />,
  search: <Search />,
  filter: <Filter />,
}
